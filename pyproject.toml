[project]
name = "hp-dl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "click>=8.1.8",
    "requests>=2.32.4",
    "tqdm>=4.67.3",
]

[project.scripts]
hp-dl = "hp_dl"


[tool.hatch.build.targets.wheel]
include = ["hp_dl/html/"]
